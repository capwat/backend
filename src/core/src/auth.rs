/// A parsed token that contains data to identify the user
/// who held the token to. This is sort of an easy way to
/// authenticate/identify the user without relying on
/// querying the database to find the exact token and grab
/// the info.
#[derive(Debug)]
pub struct JWT {}
