mod error;

// pub mod data;
// pub mod error;

// pub use data::Data;
// pub use error::Error;

// pub type Result<T, E> = std::result::Result<T, Error<E>>;

// pub mod dev {
//   pub use async_trait;
//   pub use capwat_types as types;
//   pub use error_stack;
// }
